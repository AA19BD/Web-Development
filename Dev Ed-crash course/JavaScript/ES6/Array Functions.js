const shopping_list = ["cow", "milk", "eggs"];

shopping_list.forEach((items) => {
  console.log(items);
});

//filter(same with map)
shopping_list.filter((items) => item !== eggs);
console.log(shopping_list);
